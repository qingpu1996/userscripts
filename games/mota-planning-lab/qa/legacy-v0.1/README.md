# v0.1 历史资料

此目录只保留 v0.1 时期的用户交接证据，不属于 Protocol v2 活跃 fixture。

- `current-4f-baseline.json` 仅记录用户当时明确给出的现场数值，不是真实地图快照。
- v2 运行时、fixture validator 和自动化测试不读取本目录。
- 固定 4F、HP 208、11×11 或 slot 8 都不是 v2 的运行时设计前提。
